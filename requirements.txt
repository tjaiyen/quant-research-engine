# Quant Tracker — Obsidian-native paper-trading research engine.
# Ported from quant-cockpit MINUS the Dash/Plotly web shell and the Fly/gunicorn
# production-hardening stack (dash, plotly, gunicorn, flask-limiter,
# prometheus-flask-exporter, python-json-logger were dropped — no web server).
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<3.0.0
requests>=2.31.0,<3.0.0
python-dotenv>=1.0.1,<2.0.0
yfinance>=0.2.40,<1.0.0
hmmlearn>=0.3.0,<0.4.0
# Screener signal models
statsmodels>=0.14.0,<1.0.0   # ARIMA + ADF (signals/arima_signal.py)
arch>=6.0.0,<8.999.0         # GARCH(1,1)  (signals/garch_signal.py)
pykalman>=0.9.7,<2.0.0       # optional — Kalman signal falls back to EMA if absent
joblib>=1.3.0,<2.0.0         # HMM model persistence (atomic_write_model)
# Paper auto-trader (paper-only via mock broker by default)
alpaca-trade-api>=3.1.0,<4.0.0   # only used when ALPACA_USE_MOCK=false
tenacity>=8.2.0,<10.0.0          # API retry on broker calls
ratelimit>=2.2.1,<3.0.0          # Alpaca rate limit
python-dateutil>=2.8.0,<3.0.0    # relativedelta for paper-trade gate
pytz>=2023.3                     # ET timezone for submission window
# Dev / test
pytest>=8.0.0,<10.0.0
mypy>=1.10.0,<3.0.0
