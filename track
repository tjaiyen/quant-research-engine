#!/usr/bin/env bash
# Convenience wrapper: run the `track` CLI with the repo's venv interpreter.
# Usage: ./track <doctor|refresh|screen|paper|report|status> [args]
set -euo pipefail
HERE="$(cd "$(dirname "$0")" && pwd)"
exec "$HERE/.venv/bin/python" -m cli.track "$@"
