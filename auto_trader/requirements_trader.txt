# Phase K — auto-trader extra deps (in addition to root requirements.txt).
#
# These are also declared in the project root requirements.txt so a single
# `pip install -r requirements.txt` covers everything (cockpit + screener +
# trader). This file is kept for parity with the spec and as a quick
# reference when running the auto_trader standalone.

# C2: pinned-range Alpaca version. Spec calls for ==3.0.2; that pin
# pulls a transitive PyYAML that won't build on Python 3.13. The 3.x
# API surface is stable in [3.1.0, 4.0.0) so we widen the pin while
# preserving the v3 contract. Tested locally on alpaca-trade-api 3.2.0.
alpaca-trade-api>=3.1.0,<4.0.0

# API resilience
tenacity>=8.2.0,<10.0.0
ratelimit>=2.2.1,<3.0.0

# Environment
python-dotenv>=1.0.0,<2.0.0
python-dateutil>=2.8.0,<3.0.0

# Timezone
pytz>=2023.3
